1.   var- Function scoped     Redeclaration-Allowed

let- Block scoped
Redeclaration-not allowed

const- Block scoped
Redeclaration-not allowed


2. map()	Transforms every element	A new array

forEach()	Performs an action for each element	undefined

filter()	Selects elements that match a condition	A new array

3. Arrow functions provide a shorter syntax for writing functions

4. Destructuring lets you extract values from arrays or properties from objects into separate variables.


5. Template literals use backticks (`) instead of quotes and allow embedded expressions with ${}.